"""LLM-слой: объяснение результата ядра на естественном языке (GigaChat).

Принцип безопасности: LLM НИКОГДА не вводит новые числа и не принимает решение —
он только пересказывает структурированный результат детерминированного ядра.
Каждый ответ проверяется на «обоснованность» (groundedness): если в тексте
появляются цифры, которых нет в данных ядра, ответ отбраковывается и заменяется
детерминированным шаблоном. Это ключевой механизм контроля модельного риска LLM.
"""
